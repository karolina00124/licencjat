{% extends 'app_base.html.twig' %}

{% block title %}
    {{ 'title_event_edit'|trans({'%id%': event.id|default('')}) }}
{% endblock %}

{% block body %}
    <div class="index_title">{{ 'title_event_edit'|trans({'%id%': event.id|default('')}) }}
        {{ form_start(form, { method: 'PUT', action: url('event_edit', {id: event.id}), attr: {'novalidate': 'novalidate'} }) }}</div>

    <div class="container">
        <div class="row">
            <div class="col col-lg">
                <div class="form-group">
                    {{ form_row(form.title, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group">
                    {{ form_row(form.description, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group date-container">
                    {{ form_row(form.start_time, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group date-container">
                    {{ form_row(form.end_time, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group">
                    {{ form_row(form.todolist, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group row float-sm-right">
                    <input type="submit" value="{{ 'action_save'|trans }}" class="btn btn-primary" />
                </div>
                <div class="form-group row float-sm-left">
                    <a href="{{ url('event_index') }}" class="btn btn-light">
                        {{ 'action_back_to_list'|trans }}
                    </a>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
